{
  "mcpServers": {
    "bda": {
      "command": "python",
      "args": ["-m", "bda_mcp_server"],
      "env": {
        "AWS_REGION": "${AWS_REGION}",
        "BDA_PROJECT_ARN": "${BDA_PROJECT_ARN}"
      }
    }
  }
}
